// src/lib/api/transactions.js
import { mockTransactions } from '../mock_temp/transactions'

// Placeholder API layer – not used yet
// You can import and use this later when you really need fetch.
export async function fetchTransactions() {
  return mockTransactions
}
