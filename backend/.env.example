SECRET_KEY=secret-key-for-staging/production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# CORS - Frontend URLs
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000

# Make.com Webhooks
INGESTION_API_KEY=make-webhook-api-key

# Email Owner Configuration (Comma-separated emails that can use Google OAuth)
ALLOWED_EMAILS=owner@example.com,admin@example.com

# Google OAuth Configuration
GOOGLE_OAUTH_CLIENT_ID=google-client-id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=google-client-secret
GOOGLE_OAUTH_CALLBACK_URL=http://localhost:8000/auth/google/callback/

# Render Deployment (leave empty for development)
RENDER_EXTERNAL_HOSTNAME=
